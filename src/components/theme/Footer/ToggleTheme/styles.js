import styled from 'styled-components';

export const Wrapper = styled.button`
  background: none;
  border: none;
  cursor: pointer;
  filter: invert(1);
  transition: 0.3s all;
  margin-right: 3.2rem;
  img {
    margin-bottom: unset;
  }
`;
